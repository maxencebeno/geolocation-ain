{% trans_default_domain 'suppression' %}

{% block body %}

    <div class="row">
        <div class="col-sm-10 col-sm-offset-1">
            <h1>{{ 'suppression.title'|trans }}</h1>
            <hr>

            {% form_theme form 'bootstrap_3_layout.html.twig' %}

            {{ form(form, {'action': path('user_ask_delete_account'), 'method': 'POST'}) }}
        </div>
    </div>

{% endblock %}
