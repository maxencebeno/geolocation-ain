<?php

namespace Geolocation\AdminBundle\Repository;

/**
 * GroupeRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class GroupeRepository extends \Doctrine\ORM\EntityRepository
{
    public function findByUserExist()
    {
        $qb = $this->createQueryBuilder('g');
        $qb->join('GeolocationAdminBundle:Cpf', 'c', 'WITH', 'g.id = c.section')
            ->join('GeolocationAdminBundle:Ressources', 'r', 'WITH', 'c.id = r.cpf')
            ->groupBy('g.id');

        return $qb->getQuery()->getResult();
    }
}
