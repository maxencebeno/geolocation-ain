{# empty Twig template #}
<header class="">

    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                        data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="{{ path('site_homepage') }}">GEORIC</a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

                <div class="navbar-right">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <a href="{{ path('fos_user_profile_show') }}">
                            <button type="button" class="btn btn-default navbar-btn">Mon profil</button>
                        </a>
                        <a href="{{ path('fos_user_security_logout') }}">
                            <button type="button" class="btn btn-default navbar-btn">Déconnexion</button>
                        </a>
                        {% if is_granted('ROLE_SUPER_ADMIN') %}
                            <a href="{{ path('geolocation_admin_homepage') }}">
                                <button type="button" class="btn btn-default navbar-btn">Administration</button>
                            </a>
                        {% endif %}
                    {% else %}
                        <a href="{{ path('pre_register') }}">
                            <button type="button" class="btn btn-default navbar-btn">S'inscrire</button>
                        </a>
                        <a href="{{ path('fos_user_security_login') }}">
                            <button type="button" class="btn btn-default navbar-btn">Se connecter</button>
                        </a>
                    {% endif %}
                    {% if is_granted('ROLE_PREVIOUS_ADMIN') %}
                        <a href="{{ path('site_homepage', {'_switch_user': '_exit'}) }}">
                            <button type="button" class="btn btn-default navbar-btn">Quitter l'utilisateur</button>
                        </a>
                    {% endif %}
                </div>

            </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
    </nav>


</header>